<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/group_39"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/group_39"
        android:supportsRtl="true"
        android:theme="@style/Theme.Isana"
        tools:targetApi="31">
        <activity
            android:name=".modules.Inventory.Inventory"
            android:exported="false" />
        <activity
            android:name=".modules.menus.Home"
            android:exported="false" />
        <activity
            android:name=".modules.main.view.MainView"
            android:exported="false" />
        <activity
            android:name=".modules.InventoryHistory.InventoryHistory"
            android:exported="false" />
        <activity
            android:name=".modules.init.InitView"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>